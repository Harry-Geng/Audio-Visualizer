# Python
.venv/
__pycache__/
*.pyc

# OS / editor
.DS_Store

# Claude Code local (machine-specific permissions)
.claude/settings.local.json

# Ingest scratch
_incoming/

# ── Audio data: large + regenerable by re-ingesting, never commit ──
*.flac
*.wav
*.mp3
*.m4a
*.aac
*.ogg
*.aiff
*.aif
*_stems/
*_stems_hq/
*_features.json
# moment-index artifacts (per-song, regenerable, ~1MB each)
*_moments.npz
*_moments.json
# lyrics (per-song, regenerable via backfill_lyrics.py)
*_lyrics.json
# CLAP text-search embeddings (per-song) + galaxy-map layout — regenerable
*_clap.npz
galaxy.npz

# logs
*.log

# batch runner: secrets + run log + playlist exports (personal track lists)
spotify_creds.json
spotify_token.json
batch_log.jsonl
*.csv
